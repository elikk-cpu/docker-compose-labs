# Tagging strategy

Подготовь минимум:

- semantic version: `0.1.0`;
- immutable training build tag: имитация Git SHA;
- optional moving tag `dev` — только как дополнительный указатель.

Не используй `latest` как deployment contract.
Для строгой воспроизводимости изучи digest после push.
