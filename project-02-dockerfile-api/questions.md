# Вопросы

1. Чем `ARG` отличается от `ENV`?
2. Что такое build context?
3. Как `.dockerignore` влияет на безопасность и cache?
4. Почему `0.0.0.0`, а не `127.0.0.1`?
5. Чем `CMD` отличается от `ENTRYPOINT`?
6. Почему exec-форма лучше принимает Unix signals?
7. Какие инструкции инвалидировали cache во второй сборке?
