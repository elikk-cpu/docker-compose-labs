После получения рабочего стека воспроизведи и исправь:

1. Backend использует `localhost` вместо `database`.
2. Неправильный target port frontend proxy.
3. PostgreSQL volume сохраняет старые данные.
4. Healthcheck проверяет неверный endpoint.
5. Secret не выдан backend.
6. Production override случайно возвращает source bind mount.
7. Worker запускается от root.
8. Registry tag отсутствует.
