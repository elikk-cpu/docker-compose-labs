services:
  backend:
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/wrong-endpoint', timeout=2)
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 5s
