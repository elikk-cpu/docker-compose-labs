services:
  app:
    env_file:
      - ./config/prod.env
    ports:
      - "127.0.0.1:${APP_PORT}:5000"
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true

  database:
    restart: unless-stopped
