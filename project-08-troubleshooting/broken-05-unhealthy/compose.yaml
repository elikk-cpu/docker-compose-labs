services:
  app:
    build: .
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - |
          import urllib.request
          urllib.request.urlopen("http://localhost:8000/readyz", timeout=2)
      interval: 5s
      timeout: 3s
      retries: 3
