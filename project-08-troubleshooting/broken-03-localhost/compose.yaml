services:
  database:
    build:
      context: .
      dockerfile: Dockerfile.server

  api:
    build:
      context: .
      dockerfile: Dockerfile.client
    environment:
      DB_HOST: database
    depends_on:
      - database
