# Диагностические вопросы

1. Чем image отличается от container?
2. Что хранится во writable layer?
3. Почему `localhost` не указывает на соседний контейнер?
4. Почему внутренний порт — `8080`, хотя с хоста используется `8088`?
5. Чем bind mount отличается от named volume?
6. Почему read-only mount безопаснее?
7. Что покажут `logs`, `inspect`, `history` и `stats`?
8. Почему `docker stop` предпочтительнее `docker kill` при штатной остановке?
9. Что означает copy-on-write?
